class Solution {
public:
    int maxProfit(vector<int>& prices) {

        int m = prices[0];
        int M = 0;

        for(int i = 0; i < prices.size(); i++){

            m = min(m, prices[i]);
            M = max(M, abs(prices[i] - m));

        }

        return M;
        
    }
};