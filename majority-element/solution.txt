class Solution {
public:
    int majorityElement(vector<int>& nums) {

        int ct = 0;
        int num = 0;

        for(auto it : nums){

            if(ct == 0) num = it;

            if(it == num) ct++;
            else ct--;

        }

        return num;
        
    }
};